<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pinfluence</title>
    <link rel="shortcut icon" href="/public/images/logo.png" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@3.6.0/fonts/remixicon.css" rel="stylesheet">
    <link rel="stylesheet" href="/public/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <% if(nav === true){ %>
        <div class="w-full px-5 sm:px-10 py-3 flex flex-col sm:flex-row justify-between bg-zinc-800 text-white items-center">
            <h3 class="text-lg sm:text-xl">Pinfluence</h3>
            <div class="flex gap-4 mt-3 sm:mt-0">
                <a href="/profile" class="block sm:inline">Profile</a>
                <a href="/feed" class="block sm:inline">Feed</a>
                <a href="/logout" class="px-3 py-2 bg-red-700 text-white rounded-lg">Logout</a>
            </div>
        </div>

        <% } %>